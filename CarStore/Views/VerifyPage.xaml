<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CarStore.Views.VerifyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CarStore.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
        Grid.Row="0"
        x:Uid="XÁC NHẬN TÀI KHOẢN"
        Style="{StaticResource PageTitleStyle}" />

        <StackPanel Grid.Row="1" VerticalAlignment="Center" Height="100">
            <TextBlock HorizontalAlignment="Center" Text="Tên đăng nhập"/>
            <TextBox HorizontalAlignment="Center" Width="200" x:Name="name"/>
            <HyperlinkButton Content="Click to go to page 2"
                          Command="{x:Bind ViewModel.VerifyCommand}"
                          HorizontalAlignment="Center"/>
        </StackPanel>

    </Grid>
</Page>
